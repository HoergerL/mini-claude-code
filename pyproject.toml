[project]
name = "mini-claude-code"
version = "0.1.0"
description = "A minimal agentic coding assistant powered by an LLM with Read, Write, and Bash tools."
requires-python = ">=3.11"
dependencies = [
    "openai>=2.15.0",
]
